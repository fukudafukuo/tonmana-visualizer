# Chrome Web Store 公開ガイド

## 事前準備

### 1. デベロッパー登録（初回のみ・$5）
1. https://chrome.google.com/webstore/devconsole にアクセス
2. Google アカウントでログイン
3. デベロッパー登録料 $5 を支払う
4. 連絡先メールアドレスを登録

### 2. プライバシーポリシーの公開
`privacy-policy.html` を公開URLでホスティングする必要があります。

**方法A: GitHub Pages（推奨・無料）**
1. GitHubリポジトリの Settings → Pages で有効化
2. `privacy-policy.html` をリポジトリのルートに配置
3. `https://<username>.github.io/<repo>/privacy-policy.html` でアクセス可能になる

**方法B: 自前サーバー**
任意のWebサーバーに `privacy-policy.html` を配置

### 3. スクリーンショットの準備
Chrome Web Store では以下の画像が必要です：

| 種類 | サイズ | 必須 |
|------|--------|------|
| スクリーンショット | 1280×800 または 640×400 | ✅ 最低1枚 |
| プロモーション画像（小） | 440×280 | 推奨 |
| プロモーション画像（大） | 920×680 | 任意 |
| マーキー画像 | 1400×560 | 任意 |

**スクリーンショットの撮り方：**
1. 拡張機能をインストールした状態でサイドパネルを開く
2. 適当なWebサイト（例: apple.com）で解析を実行
3. サイドパネルに結果が表示された状態でスクリーンショット
4. 画像編集ツールで 1280×800 にリサイズ

---

## パッケージング

```bash
npm run package
```

これで `tonmana-visualizer.zip` がプロジェクトルートに生成されます。

---

## 公開手順

### ステップ 1: アイテムを追加
1. [Developer Dashboard](https://chrome.google.com/webstore/devconsole) を開く
2. 「新しいアイテム」をクリック
3. `tonmana-visualizer.zip` をアップロード

### ステップ 2: ストア掲載情報を入力
`store-listing.md` の内容を以下のフィールドに貼り付けます：

- **名前**: トンマナビジュアライザー
- **概要**: store-listing.md の「概要」をコピー
- **説明**: store-listing.md の「説明」をコピー
- **カテゴリ**: デベロッパー ツール
- **言語**: 日本語
- **スクリーンショット**: 準備した画像をアップロード

### ステップ 3: プライバシーへの取り組み
- **単一用途の説明**: 「Webページのスタイル情報（CSS）を解析し、トーン＆マナーとして可視化します」
- **権限の正当性**:
  - activeTab: ユーザーが閲覧中のページのスタイルを解析するため
  - tabs: タブ切り替え時に解析対象を追跡するため
  - scripting: ページ内でスタイル解析スクリプトを実行するため
  - host_permissions: 任意のWebページでスタイル解析を可能にするため
- **プライバシーポリシーURL**: ホスティングしたURLを入力
- **データ使用に関する開示**:
  - 「個人を特定できる情報を収集しない」を選択
  - 「ウェブ閲覧アクティビティ」→「ウェブページのコンテンツ」にチェック
  - 用途: 「アプリの機能」

### ステップ 4: 審査に提出
1. すべてのフィールドを入力したことを確認
2. 「審査のために送信」をクリック
3. 審査には通常 1〜3 営業日かかります

---

## 審査後

- 承認されるとストアに公開されます
- 拒否された場合はメールで理由が届きます（権限の説明不足が最も多い）
- ダッシュボードでインストール数・評価を確認できます

## バージョンアップ時

1. `manifest.json` と `package.json` の version を更新
2. `npm run package`
3. ダッシュボードで新しいZIPをアップロード
4. 再審査（通常 1〜3 営業日）
